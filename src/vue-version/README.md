# 加密货币交易所登录注册页面 - Vue 3 版本

## 功能特性

### 🎨 设计特色
- **深色主题**：专业的加密货币交易所风格
- **3D 粒子动画背景**：使用 Canvas 实现的动态粒子效果
- **流畅的页面切换**：Vue Transition 动画系统
- **响应式设计**：完美适配移动端和桌面端

### 🔐 认证功能
- **三步式登录流程**
  1. 邮箱输入页面
  2. 验证码验证页面
  3. 登录成功页面
- **第三方登录**：支持 Google 登录
- **验证码输入**：6 位数字验证码，支持粘贴

### 🧩 组件结构
- `App.vue` - 主应用组件
- `SignInPage.vue` - 登录页面主组件
- `MiniNavbar.vue` - 顶部导航栏
- `AnimatedNavLink.vue` - 动画导航链接
- `CanvasRevealEffect.vue` - Canvas 粒子动画效果

## 安装和运行

### 1. 安装依赖
```bash
cd vue-version
npm install
```

### 2. 启动开发服务器
```bash
npm run dev
```

### 3. 构建生产版本
```bash
npm run build
```

### 4. 预览生产版本
```bash
npm run preview
```

## 技术栈

- **Vue 3** - 组合式 API (Composition API)
- **TypeScript** - 类型安全
- **Vite** - 快速构建工具
- **Tailwind CSS 4.0** - 样式框架
- **Canvas API** - 粒子动画效果

## 项目结构

```
vue-version/
├── components/
│   ├── SignInPage.vue          # 主登录页面
│   ├── MiniNavbar.vue          # 导航栏组件
│   ├── AnimatedNavLink.vue     # 动画链接组件
│   └── CanvasRevealEffect.vue  # 粒子动画组件
├── styles/
│   └── globals.css             # 全局样式
├── App.vue                     # 应用根组件
├── main.ts                     # 应用入口
├── index.html                  # HTML 模板
├── vite.config.ts              # Vite 配置
├── tailwind.config.js          # Tailwind 配置
├── tsconfig.json               # TypeScript 配置
└── package.json                # 项目依赖
```

## 使用说明

### 登录流程

1. **邮箱输入**
   - 输入邮箱地址
   - 点击右侧箭头按钮或按回车提交
   - 触发粒子动画切换

2. **验证码输入**
   - 自动聚焦到第一个输入框
   - 输入数字后自动跳转到下一个框
   - 支持粘贴 6 位验证码
   - 按 Backspace 返回上一个框
   - 点击"返回"按钮回到邮箱输入页面

3. **登录成功**
   - 显示成功图标和欢迎信息
   - 点击"进入控制台"按钮（可自定义跳转）

### 自定义配置

#### 修改粒子动画参数
在 `SignInPage.vue` 中调整 `CanvasRevealEffect` 组件的 props：

```vue
<CanvasRevealEffect
  :animation-speed="3"      // 动画速度
  :dot-size="6"             // 粒子大小
  :colors="[[255, 255, 255]]" // 粒子颜色 (RGB)
  :reverse="false"          // 是否反向运动
/>
```

#### 修改导航链接
在 `MiniNavbar.vue` 中修改 `navLinksData` 数组：

```typescript
const navLinksData = [
  { label: '关于我们', href: '#1' },
  { label: '招聘', href: '#2' },
  { label: '发现', href: '#3' },
]
```

#### 添加登录逻辑
在 `SignInPage.vue` 中修改相关函数：

```typescript
// 邮箱提交
const handleEmailSubmit = async () => {
  // TODO: 调用 API 发送验证码
  // await sendVerificationCode(email.value)
  // ...
}

// 验证码提交
const handleCodeSubmit = async () => {
  // TODO: 调用 API 验证验证码
  // await verifyCode(email.value, code.value.join(''))
  // ...
}
```

## 样式定制

### 颜色主题
在 `styles/globals.css` 中修改 CSS 变量：

```css
:root {
  --background: #ffffff;
  --foreground: oklch(0.145 0 0);
  --primary: #030213;
  /* 更多颜色变量... */
}

.dark {
  --background: oklch(0.145 0 0);
  --foreground: oklch(0.985 0 0);
  /* 暗色主题变量... */
}
```

### Tailwind 自定义
在 `tailwind.config.js` 中扩展主题：

```javascript
export default {
  theme: {
    extend: {
      colors: {
        // 自定义颜色
      },
      animation: {
        // 自定义动画
      }
    },
  },
}
```

## 浏览器兼容性

- Chrome/Edge 90+
- Firefox 88+
- Safari 14+

## 性能优化建议

1. **懒加载组件**：对于大型应用，使用 `defineAsyncComponent` 懒加载组件
2. **减少粒子数量**：在移动端可以减少粒子数量提升性能
3. **优化动画**：使用 CSS transform 和 opacity 实现动画，避免重排重绘

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！
