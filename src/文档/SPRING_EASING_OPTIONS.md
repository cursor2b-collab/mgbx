# 🎨 弹性缓动函数选项

## 常见弹性缓动曲线对比

### 1️⃣ **轻微回弹** （推荐 - iOS风格）
```css
cubic-bezier(0.34, 1.56, 0.64, 1)
```
- **特点**：轻微超调（约10-15%），快速稳定
- **适用**：导航切换、选项卡指示器
- **视觉**：自然、优雅、不夸张
- **时长**：500ms

### 2️⃣ **标准弹性** （Material Design）
```css
cubic-bezier(0.68, -0.55, 0.265, 1.55)
```
- **特点**：明显回弹（约20-25%），双向弹跳
- **适用**：按钮动画、卡片展开
- **视觉**：生动、有趣
- **时长**：600ms

### 3️⃣ **强回弹**
```css
cubic-bezier(0.47, 1.64, 0.41, 0.8)
```
- **特点**：较强回弹（15-20%）
- **适用**：引人注目的交互
- **视觉**：活泼、科技感
- **时长**：500ms

### 4️⃣ **极致回弹** （游戏化）
```css
cubic-bezier(0.68, -0.55, 0.265, 2.55)
```
- **特点**：夸张回弹（30%+），长时间震荡
- **适用**：游戏UI、趣味交互
- **视觉**：卡通、夸张
- **时长**：700ms

### 5️⃣ **平滑标准** （无回弹）
```css
cubic-bezier(0.4, 0, 0.2, 1)
```
- **特点**：无回弹，纯加减速
- **适用**：正式界面、传统应用
- **视觉**：专业、稳重
- **时长**：400ms

## 🎯 推荐配置（根据场景）

### 移动端导航栏滑块
```css
/* 选项A：轻微回弹（推荐）*/
transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1)

/* 选项B：无回弹（保守）*/
transform 0.4s cubic-bezier(0.4, 0, 0.2, 1)

/* 选项C：适中回弹（平衡）*/
transform 0.5s cubic-bezier(0.47, 1.64, 0.41, 0.8)
```

## 📊 参数解析

```
cubic-bezier(P1x, P1y, P2x, P2y)
```

| 参数 | 范围 | 作用 | 回弹效果 |
|------|------|------|----------|
| P1x | 0-1 | 起始速度 | 越小越快启动 |
| P1y | **任意** | 起始加速度 | **>1 产生回弹** |
| P2x | 0-1 | 结束速度 | 越大越慢结束 |
| P2y | **任意** | 结束减速度 | **<0 或 >1 产生回弹** |

### 回弹量计算

```
超调百分比 ≈ |P1y - 1| × 20% + |P2y - 1| × 10%

示例：
cubic-bezier(0.34, 1.56, 0.64, 1)
超调 ≈ (1.56 - 1) × 20% = 11.2%

cubic-bezier(0.47, 1.64, 0.41, 0.8)  
超调 ≈ (1.64 - 1) × 20% = 12.8%
```

## 🧪 测试建议

在线工具：https://cubic-bezier.com/

测试配置：
1. 将曲线参数输入工具
2. 观察动画预览
3. 调整时长（建议400-600ms）
4. 在真机上测试实际效果

## 💡 微调技巧

### 增加回弹强度
```css
/* 原始 */
cubic-bezier(0.34, 1.56, 0.64, 1)

/* 增强（调高P1y）*/
cubic-bezier(0.34, 1.8, 0.64, 1)
```

### 减少回弹强度
```css
/* 原始 */
cubic-bezier(0.34, 1.56, 0.64, 1)

/* 减弱（降低P1y）*/
cubic-bezier(0.34, 1.3, 0.64, 1)
```

### 加快稳定速度
```css
/* 原始 */
cubic-bezier(0.34, 1.56, 0.64, 1)

/* 快速稳定（调高P2y）*/
cubic-bezier(0.34, 1.56, 0.64, 1.1)
```

## 🎬 实际应用示例

### iOS Safari Tab Bar
```css
cubic-bezier(0.32, 1.25, 0.375, 1.125)
```

### Material Design Emphasized
```css
cubic-bezier(0.0, 0.0, 0.2, 1)
```

### Framer Motion Spring
```css
cubic-bezier(0.5, 1.75, 0.75, 1)
```

### Vue.js Transition
```css
cubic-bezier(0.25, 0.46, 0.45, 0.94)
```

## 🏆 最终推荐

### 移动端底部导航滑块
```css
.glider-mobile.glider-animated {
    /* 推荐：轻微回弹，iOS风格 */
    transition: transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1),
                width 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
}
```

**理由**：
- ✅ 轻微回弹，不过度夸张
- ✅ 符合iOS原生体验
- ✅ 快速稳定，不拖沓
- ✅ 适合频繁切换的导航场景

---

**创建时间**：2025-11-05  
**用途**：优化移动端导航栏滑块动画  
**参考**：iOS Human Interface Guidelines、Material Design
